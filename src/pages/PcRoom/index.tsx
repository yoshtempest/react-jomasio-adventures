import { createScenePage } from "@/utils/createScenePage";
import { PcRoomScene } from "@/features/pcRoom";

// eslint-disable-next-line react-refresh/only-export-components
export default createScenePage(PcRoomScene);
