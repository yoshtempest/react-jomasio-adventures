import { createDoorTile } from "@/scenes/shared/factories";
import { CANTINA_ROUTES } from "@/scenes/shared/routes";

export const cafeteriaFourTiles = [
    createDoorTile(8, 13, CANTINA_ROUTES.TWO),
    createDoorTile(9, 13, CANTINA_ROUTES.TWO),
    createDoorTile(10, 13, CANTINA_ROUTES.TWO),
];
