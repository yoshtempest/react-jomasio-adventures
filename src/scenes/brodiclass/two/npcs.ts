import { npcPath } from "@/utils/paths";

import { createNpc } from "@/scenes/shared/factories";

export const cantinaOneNpcs = [
  createNpc(npcPath("/jhowsimar/default.svg"), 9, 4),
  createNpc(npcPath("/brothers/default.svg"), 18, 3),
];
