import { createDoorTile } from "@/scenes/shared/factories";
import { HALL_ROUTES } from "@/scenes/shared/routes";

export const pcRoomSixTiles = [
  createDoorTile(3, 2, HALL_ROUTES.AFTER_PCROOM_ONE),
];
